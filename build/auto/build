#!/bin/bash
# shirOS — live-build wrapper
# Loguea a build.log para debug. Usa pipefail para que un fallo de
# `lb build` no quede tapado por el exit code 0 de `tee`.

set -e
set -o pipefail

lb build noauto "${@}" 2>&1 | tee build.log
